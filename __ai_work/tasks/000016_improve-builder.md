# 000018 improve-builder

Improve the app/builder

View Json can become a tab inside the field panel to the left, the json can be within the panel, and have a option to open a modal

The download button should be a download icon 

The exclude form field can be a trash icon

It should have a preview button/action as well

The form content as json, we should be able to see it as yaml as well

So, make the UX better on the app builder