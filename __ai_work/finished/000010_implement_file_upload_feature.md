# Task 000010

You should implement a feature to upload files, this will upload files using blobs on postgres, and multipart form

Under pkg, create a feature named files

We should be able to create, read and remove a file

The api should support multipart file uploads

We are suppose to read a file buffer using app_url/files/file.id

You should also add a migration to create a files table, figure out by yourself the fields needed.



