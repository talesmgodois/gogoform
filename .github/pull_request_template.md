## Database changes

- [ ] Migration added to both `db/postgres/migrations` and `db/sqlite/migrations`
- [ ] Queries changed in both `db/postgres/queries` and `db/sqlite/queries`
- [ ] `make sqlc` run
